-------------------------------------------
-- LIBs
-------------------------------------------

-- CORONA LIBS
composer = 			require( "composer" 		 ) -- lets use composer instead storyboard
widget =          	require( "widget"            )
facebook =        	require( "facebook"          )
json =            	require( "json"              )
physics =         	require( "physics"           )

-- MY LIBS
gameaudio =		  	require( "src.infra.gameaudio" 			)
easingx  =        	require( "src.infra.easing"            	)
movieclip =       	require( "src.infra.movieclip"         	)
globaldefinitions = require( "src.infra.globaldefinitions" 	)
